{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Git Submodule Sync",
            "type": "shell",
            "command": "cd zazlib && git add . && git commit -m 'update' && git push origin HEAD:main && cd .. && git add zazlib && git commit -m 'sync submodule' && git push origin main",
            "problemMatcher": []
        }
    ]
}